<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.rmznshn.rssokuyucu">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Giris" android:label="@string/girissayfasi" android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" android:label="@string/haberkaynaklari" android:screenOrientation="portrait"/>
        <activity android:name=".Haberler" android:label="@string/haberbasliklari" android:screenOrientation="portrait"/>
        <activity android:name=".WebGoruntule" android:screenOrientation="portrait" />
    </application>

</manifest>
